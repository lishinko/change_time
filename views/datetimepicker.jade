div.container
    div.row
        div.col-sm-6
            div.form-group
                div#datetimepicker.input-group.date
                    input.form-control
                    span.input-group-addon
                        span.glyphicon.glyphicon-calendar
                button#submit-time.btn.btn-default 提交
        script.
            $(function () {
                $('#datetimepicker').datetimepicker({
                    // locale: 'ru'
                    format:"dddd, YYYY/MM/DD, HH:mm:ss z"
                });
                $('#submit-time').click( function(){
                    var date = $('#datetimepicker :input').val();
                    alert(date);
                })
            });